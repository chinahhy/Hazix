{
  "name": "hdao-web-preview",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && for file in public/*.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "hls.js": "1.7.3"
  }
}
