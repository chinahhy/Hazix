# 路线图（ROADMAP）

按"用户能多直接感觉到"排序。每项写明现状、改法与验收方式，方便任何人（人或代理）接手。

**硬性约定**：任何 Kotlin 改动都必须走云端 CI 验证，本机编译不了——
项目路径含中文会让 Compose 编译器插件路径被转义破坏，详见 `HANDOFF.md` 第 1 节。

## 已完成

- [x] v3.4.0：动漫播放控制条自动隐藏；分类页从详情返回时恢复滚动与焦点；
      新增可发现的「检查更新」入口；TV 与手机统一版本号与签名，一次构建双端。
- [x] 删除未参与构建的遗留 `app/` 模块（758 行死代码 + 仓库内唯一的第三方 MIT 代码）。
- [x] v3.5.0：**返回后状态保持做成通用机制**，首页、搜索页、分类页三处统一；
      搜索词与结果不再丢失，首页不再重新加载与跳回顶部。
- [x] v3.5.0：**播放器字幕 / 音轨 / 倍速**，菜单键或长按 OK 打开播放设置面板。
- [x] v3.6.0：**收藏**（详情页开关 + 首页「我的收藏」行）与**搜索历史**（最近 8 条可一键复用），
      并为其纯逻辑补了单元测试（`UserCollectionsTest`，6 个用例，由 CI 保证）。

## P0 · 你会天天感觉到的

### 1. 收藏的进阶用法

**现状**：收藏只能从详情页加，首页只有一行「我的收藏」。

**可做**：长按海报直接收藏/取消（不用进详情）；收藏页单独成页并支持排序（最近添加 / 评分 / 年份）；
收藏的节目有新集时在首页提示。（需要后端接口支持"更新到第几集"才能做追更提醒。）

## P1 · 工程质量

### 2. 字符串资源化

154 处中文硬编码在 Kotlin 里，`strings.xml` 只有 `app_name`。
搬进资源后可改文案、可加语言，并消掉一批 lint 警告。（被删掉的旧 `app/` 模块反而有 `values-zh/`。）

### 3. 签名密钥决策（**对外分发前必须决定**）

当前用 debug 风格密钥（密码 `android`、别名 `androiddebugkey`，明文写在公开仓库），
唯一保密的是 CI 里的 keystore 文件。**换钥匙会让已装用户无法覆盖升级**，
所以要么现在换，要么接受这把钥匙终身有效。

### 4. 依赖集中管理 + 打开升级通道

没有 `libs.versions.toml`；两个 Dependabot 升级 PR 是红的，
AGP 8.6.1 / Kotlin 2.0.20 只会越来越旧。

### 5. 真机 UI 测试

`androidTest` 目录为空，焦点、返回、播放器这类"看出来的"行为只能靠人工发现——
分类页返回焦点那个 bug 就是这么一路发到 v3.3.9 的，"返回丢状态"更是在三个页面各出现一次。
纯逻辑现在有单元测试兜底（见 `UserCollectionsTest`、`WatchProgressTest`），UI 行为还没有。

### 6. 播放器轨道切换的真实片源验证

`PlayerSettings.kt` 用 `TrackSelectionParameters` 覆盖字幕/音轨选择，
只在 CI 里过了编译与 lint，**从未在真实媒体流上验证**。
需要确认：切换字幕不黑屏、关闭字幕后界面恢复、多音轨片源能切、倍速不引发音画不同步。
若出现异常，优先怀疑这里。

## P2 · 平台与打磨

- 手机端没有应用内更新，也没有收藏与搜索历史（这两项只在电视端实现了）。
- **播放器缺少 DNS 兜底路径**：内容接口（`HdaoApi`）已有"加密 DNS 失败后换系统 DNS 重试"两条路径，
  但播放器用的 `OkHttpDataSource` 只挂了加密 DNS 客户端；若接口靠兜底能通而播放不能，就是这里。
- **`NetworkClientsTest` 会访问真实网络**（请求 hdao.tv）。站点若限制 GitHub Actions 出口 IP，
  CI 会无故变红。应把网络断言拆成可选/集成测试，只保留纯逻辑断言。
- 播放器可再加：清晰度选择、画面比例、跳过片头、长按快进。
- 可访问性：字号设置、更完整的 `contentDescription`。
- **三套界面重复**（`web/` 预览 336 行 JS + 电视端 2200+ 行 + 手机端 937 行）：
  长期维护成本最高的一项，短期不建议动，但新增交互要意识到要改三处。
- 首页 Hero 的选中项在返回后不保留（因为每 8 秒自动轮播，故意不恢复）。
