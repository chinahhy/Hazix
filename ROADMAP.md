# 路线图（ROADMAP）

按"用户能多直接感觉到"排序。每项写明现状、改法与验收方式，方便任何人（人或代理）接手。

**硬性约定**：任何 Kotlin 改动都必须走云端 CI 验证，本机编译不了——
项目路径含中文会让 Compose 编译器插件路径被转义破坏，详见 `HANDOFF.md` 第 1 节。

## 已完成

- [x] v3.4.0：动漫播放控制条自动隐藏；分类页从详情返回时恢复滚动与焦点；
      新增可发现的「检查更新」入口；TV 与手机统一版本号与签名，一次构建双端。
- [x] 删除未参与构建的遗留 `app/` 模块（758 行死代码 + 仓库内唯一的第三方 MIT 代码）。

## P0 · 你会天天感觉到的

### 1. 返回后状态保持（做成通用机制）

**现状**：分类页已修（`CategoryScreenState`）。另外两页还是同一个病：

- **搜索页（更严重）**：`query`、`submitted`、`results` 都是 `remember`，
  搜索 → 进详情 → 返回，**搜索词和结果全部消失**，得重新搜。
- **首页**：`state` 是 `remember` 且 `LaunchedEffect(retry)` 每次重新进入都会重跑，
  从详情返回时会闪一下"正在准备片库…"并重建首页（轮播与滚动位置重置）。

**约定**：屏幕离开组合前把状态交还给导航层（`HdaoTvApp` 持有的
`HomeScreenState` / `SearchScreenState` / `CategoryScreenState`），
返回时按 `lastOpenedVodId` 滚动回原卡片并**显式请求焦点**。
新增页面必须遵守这条，否则同类问题会再次出现。

**验收**：搜索 → 进详情 → 返回，搜索词与结果仍在、光标回到刚才那张卡片；
首页 → 进详情 → 返回，不出现加载闪屏，滚动与选中位置保持。

### 2. 播放器：字幕 / 音轨 / 倍速

**现状**：代码里这三样各 0 处。看动漫与外剧没有字幕是第一顺位痛点。

**改法**：用 Media3 的 `player.currentTracks` 枚举文本轨与音轨，
用 `TrackSelectionParameters` 设置覆盖选择（含"关闭字幕"）；倍速用 `player.setPlaybackSpeed`。

**遥控器可达性**：面板必须能从遥控器打开（长按 OK 或菜单键），
上下选行、左右改值——**不要重犯"功能存在但找不到入口"的错误**（第 3 项修复的教训）。

**验收**：真人用遥控器能在播放中切换字幕轨、关闭字幕、切音轨、在 0.5×–2× 之间调倍速。

### 3. 收藏 / 追剧 + 搜索历史

**现状**：只有"继续观看"，没有收藏，没有搜索历史。

**改法**：与 `WatchProgress` 同构的本地存储；首页加一行"我的收藏"；
搜索页记住最近若干条关键词并可一键复用。

**验收**：收藏后重启应用仍在；搜索历史可一键复用。

## P1 · 工程质量

### 4. 字符串资源化

154 处中文硬编码在 Kotlin 里，`strings.xml` 只有 `app_name`。
搬进资源后可改文案、可加语言，并消掉一批 lint 警告。（被删掉的旧 `app/` 模块反而有 `values-zh/`。）

### 5. 签名密钥决策（**对外分发前必须决定**）

当前用 debug 风格密钥（密码 `android`、别名 `androiddebugkey`，明文写在公开仓库），
唯一保密的是 CI 里的 keystore 文件。**换钥匙会让已装用户无法覆盖升级**，
所以要么现在换，要么接受这把钥匙终身有效。

### 6. 依赖集中管理 + 打开升级通道

没有 `libs.versions.toml`；两个 Dependabot 升级 PR 是红的，
AGP 8.6.1 / Kotlin 2.0.20 只会越来越旧。

### 7. 真机 UI 测试

`androidTest` 目录为空，焦点、返回、播放器这类"看出来的"行为只能靠人工发现——
分类页返回焦点那个 bug 就是这么一路发到 v3.3.9 的。补几个覆盖这些行为的测试。

## P2 · 平台与打磨

- 手机端没有应用内更新（电视端有）。
- 播放器可再加：清晰度选择、画面比例、跳过片头、长按快进。
- 可访问性：字号设置、更完整的 `contentDescription`。
- **三套界面重复**（`web/` 预览 336 行 JS + 电视端 2144 行 + 手机端 937 行）：
  长期维护成本最高的一项，短期不建议动，但新增交互要意识到要改三处。
